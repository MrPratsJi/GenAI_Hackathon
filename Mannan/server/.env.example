# Toggle MOCK mode for offline/local demo. 1 = mock replies, 0 = call Vertex AI
USE_MOCK=1

# Google Cloud (only if USE_MOCK=0)
GCLOUD_PROJECT=your-project-id
GCP_LOCATION=us-central1
VERTEX_MODEL=gemini-1.5-pro
